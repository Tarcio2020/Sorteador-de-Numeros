A configuração do Less divide a compilação em 
Desenvolvimento e produção. Vamos configurar o less para 
para que o trecho de produção seja executado apenas na version
(internet)....

1-Mudamos o psckage.json para:
    "scripts": {
    "grunt": "grunt",
    "build": "grunt build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },


2-Alteramos no final do arq Grunt:
    // [] = Array...pode conter várias tarefas
    grunt.registerTask('default', ['less: development']);
    grunt.registerTask('build', ['less: production']);


3-Alteramos a configuração:

  module .exports = function(grunt) {
    grunt.initConfig({
        pkg: grunt.file.readJSON('package.json'),
        less: {
            //máquina
            development: {
                files: {
                    'dev/styles/main.css': 'src/styles/main.less'
                }
            },
            //browser
            production: {
                options: {
                    compress: true,
                },
                files: {
                    'dist/styles/main.min.css': 'src/styles/main.less'
                }
            }
        },

    })
    grunt.loadNpmTasks('grunt-contrib-less');

    // [] = Array...pode conter várias tarefas
    //                             não pode ter espaço
    grunt.registerTask('default', ['less:development']);
    grunt.registerTask('build', ['less:production']);

}


4-Damos o comando:

  npm run grunt

5-Comando:

  npm run build


As pastas com os caminhos serão criadas:

      //máquina
            development: {
                files: {
                    'dev/styles/main.css': 'src/styles/main.less'
                }
            },
            //browser
            production: {
                options: {
                    compress: true,
                },
                files: {
                    'dist/styles/main.min.css': 'src/styles/main.less'
                }
            },


________________________________________________________________________________________

Para o Grunt observar todas alterações e compilar automáticamente:

1-Vamos instalar o pacote watch

  npm install --save-dev grunt-contrib-watch

2-Vamos até o Gruntfile.js declaramos o pacote:

  grunt.loadNpmTasks('grunt-contrib-watch')


3-Na configuração vamos criar watch após o pacote do less:
4-Alteramos o register task no final da página:

module .exports = function(grunt) {
    grunt.initConfig({
        pkg: grunt.file.readJSON('package.json'),
        less: {
            //máquina
            development: {
                files: {
                    'dev/styles/main.css': 'src/styles/main.less'
                }
            },
            //browser
            production: {
                options: {
                    compress: true,
                },
                files: {
                    'dist/styles/main.min.css': 'src/styles/main.less'
                }
            }
        },
        watch: {                                           <<<<<<<<<<<<<<<<<<<< AQUI!
            less: {
                files: ['src/styles/**/*.less/'],
                tasks: ['less:development']
            }
        }

    })





    grunt.loadNpmTasks('grunt-contrib-watch')
    grunt.loadNpmTasks('grunt-contrib-less');

    // [] = Array...pode conter várias tarefas
    //                             não pode ter espaço
    grunt.registerTask('default', ['watch']);    <<<<<<<<<<<<<<<<<<<<<<<<<<<<<<< AQUI!
    grunt.registerTask('build', ['less:production']);

}


6-De o comando:
npm run grunt

